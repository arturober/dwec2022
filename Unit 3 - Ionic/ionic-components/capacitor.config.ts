import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arturober.components',
  appName: 'Ionic Components',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
